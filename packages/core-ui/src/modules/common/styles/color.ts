import * as color from '@saashq/ui/src/styles/ecolor';

const rgb = color.rgb;
const rgba = color.rgba;
const darken = color.darken;
const lighten = color.lighten;

export { rgb, rgba, darken, lighten };
