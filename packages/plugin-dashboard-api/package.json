{
  "name": "@saashq/plugin-dashboard-api",
  "version": "1.0.0",
  "scripts": {
    "install-deps": "yarn install",
    "dev": "yarn dev",
    "build": "yarn build",
    "start": "dist/plugin-knowledgebase-api && node src"
  }
}
